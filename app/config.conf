# Database configuration
DB_HOST     = localhost     # Địa chỉ máy chủ cơ sở dữ liệu
DB_PORT     = 5432          # Cổng kết nối cơ sở dữ liệu
DB_USER     = admin         # Tên đăng nhập
DB_PASSWORD = secret        # Mật khẩu đăng nhập
